import { createTheme } from "@mui/material/styles";

export const PinfluencerMUITheme = createTheme({
  palette: {
    primary: {
      main: "#6DA66A",
      contrastText: "#fff",
    },
    disabled: {
      main: "#00000042",
    },
  },
});
